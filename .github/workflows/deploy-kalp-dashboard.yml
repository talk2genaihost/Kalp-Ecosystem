name: Deploy KALP Cinematic Studio to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'apps/cinematic-studio/**'
      - 'src/development-studio/**'
      - 'tests/**'
      - 'data/retro-64-reference.json'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy-kalp-dashboard.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Run Retro 64 gate tests
        run: npm test -- --test-name-pattern="RETRO-64 Gate v1.0"

  build:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Assemble static dashboard
        run: |
          rm -rf dist
          mkdir -p dist/data
          cp apps/cinematic-studio/index.html dist/index.html
          cp data/retro-64-reference.json dist/data/retro-64-reference.json
          touch dist/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

import type { AttemptId, ExecutionId, PlanId, RequestId, TokenCount, CostAmount } from "./common.js";
export interface ExecutionTelemetry { requestId: RequestId; decisionId: string; planId: PlanId; executionId: ExecutionId; attemptId: AttemptId; providerId: string; connectionId?: string; modelId?: string; attemptNumber: number; startedAt: string; completedAt: string; latencyMs: number; status: "success" | "failure" | "timeout" | "cancelled"; httpStatus?: number; failureCode?: string; retryAfterMs?: number; inputTokens?: TokenCount; outputTokens?: TokenCount; estimatedCost?: CostAmount; cacheHit?: boolean; fallbackTriggered?: boolean; }
